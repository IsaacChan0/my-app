import Nav from './Nav';

const Header = function()
{
    return(
        <div class="background">
                    <section class="topBar">
                    <h1 id="name">
                        Isaac Chan
                    </h1>
                    </section>
                    <Nav />
                </div>
    );
}

export default Header;